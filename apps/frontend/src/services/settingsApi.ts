import type{FrontendSettings}from'./navigation';const BASE=((import.meta as any).env?.VITE_API_URL||'').replace(/\/$/,'');async function req<T>(path:string,init?:RequestInit){const response=await fetch(`${BASE}${path}`,{...init,headers:{'Content-Type':'application/json',...init?.headers}});if(!response.ok){const text=await response.text();throw new Error(text||`Settings request failed (${response.status})`)}return response.json()as Promise<T>}export const getWorkspaceSettings=()=>req<FrontendSettings>('/admin/settings');export const updateWorkspaceSettings=(settings:FrontendSettings)=>req<FrontendSettings>('/admin/settings',{method:'PUT',body:JSON.stringify(settings)});export const resetWorkspaceSettings=()=>req<FrontendSettings>('/admin/settings/reset',{method:'POST'});export const getSettingsHistory=()=>req<{id:string;actor:string;action:string;changes:Record<string,unknown>;createdAt:string}[]>('/admin/settings/history')
