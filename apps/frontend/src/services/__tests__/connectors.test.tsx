import assert from'node:assert/strict';import{describe,it}from'node:test';import{renderToStaticMarkup}from'react-dom/server';import{ConnectorForm}from'../../components/connectors/ConnectorForm';import{ConnectorList}from'../../components/connectors/ConnectorList';import{ConnectorPreview}from'../../components/connectors/ConnectorPreview';import{ConnectorStatusBadge}from'../../components/connectors/ConnectorStatusBadge';import{ConnectionTestPanel}from'../../components/connectors/ConnectionTestPanel';import type{Connector}from'../../types/connector';import{previewConnector,syncConnector,testConnector}from'../connectorApi';const connector:Connector={id:'c1',name:'Corp AD',connectorType:'ACTIVE_DIRECTORY',status:'CONNECTED',enabled:true,configuration:{url:'ldaps://dc.example.com',baseDn:'DC=example,DC=com',bindDn:'reader',bindPassword:'********'},capabilities:[]}
describe('connector management frontend',()=>{it('renders a safe connector form without prefilling passwords',()=>{const html=renderToStaticMarkup(<ConnectorForm connector={connector}onSave={()=>{}}onCancel={()=>{}}/>);assert.match(html,/Corp AD/);assert.match(html,/type="password"/);assert.doesNotMatch(html,/value="\*\*\*\*\*\*\*\*"/)});it('shows status, preview and connection results',()=>{assert.match(renderToStaticMarkup(<ConnectorStatusBadge status="CONNECTED"/>),/CONNECTED/);assert.match(renderToStaticMarkup(<ConnectionTestPanel result={{connected:true,namingContexts:['DC=example']}}/>),/successful/);assert.match(renderToStaticMarkup(<ConnectorPreview run={{syncRunId:'r',connectorId:'c',mode:'PREVIEW',status:'COMPLETED',startedAt:'x',objectCounts:{USER:2},pageCounts:{},warnings:['Preview only'],errors:[]}}/>),/USER/)});it('renders loading-compatible empty state',()=>{assert.match(renderToStaticMarkup(<ConnectorList connectors={[]}actions={()=>({})}/>),/No connectors configured/)});it('wires test, preview, and sync API actions',async()=>{const original=fetch;const calls:string[]=[];globalThis.fetch=(async(url:any,init:any)=>{calls.push(`${init.method}:${url}`);return new Response(JSON.stringify(url.includes('/test')?{connected:true,namingContexts:[]}:{syncRunId:'r',connectorId:'c1',mode:'PREVIEW',status:'COMPLETED',startedAt:'x',objectCounts:{},pageCounts:{},warnings:[],errors:[]}),{status:200})})as any;try{await testConnector('c1');await previewConnector('c1');await syncConnector('c1');assert.ok(calls.some(call=>call.includes('/test')));assert.ok(calls.some(call=>call.includes('/preview')));assert.ok(calls.some(call=>call.includes('/sync')))}finally{globalThis.fetch=original}})})
