import { useCallback,useEffect,useState } from 'react'; import type { GraphSourceMode } from '../types/neo4j'; import { loadSettings,saveSettings } from '../services/navigation';import{getWorkspaceSettings}from'../services/settingsApi'
const KEY='iag-graph-source';const EVENT='iag-graph-source-change'
let sharedSettings:ReturnType<typeof getWorkspaceSettings>|null=null
const loadSharedSettings=()=>sharedSettings??=(getWorkspaceSettings().catch(error=>{sharedSettings=null;throw error}))
export function getStoredGraphSource(storage:Pick<Storage,'getItem'>):GraphSourceMode{const value=storage.getItem(KEY);if(value==='mock'||value==='imported'||value==='neo4j')return value;return loadSettings(storage).graphSource}
export function useGraphSource(requested?:string|null){const[source,setState]=useState<GraphSourceMode>(()=>requested==='neo4j'||requested==='imported'||requested==='mock'?requested:typeof localStorage==='undefined'?'neo4j':getStoredGraphSource(localStorage));const setSource=useCallback((next:GraphSourceMode)=>{setState(next);localStorage.setItem(KEY,next);window.dispatchEvent(new CustomEvent(EVENT,{detail:next}))},[]);useEffect(()=>{if(requested||typeof localStorage==='undefined'||localStorage.getItem(KEY))return;let cancelled=false;void loadSharedSettings().then(settings=>{if(cancelled)return;saveSettings(localStorage,settings);setState(settings.graphSource);window.dispatchEvent(new CustomEvent(EVENT,{detail:settings.graphSource}))}).catch(()=>undefined);return()=>{cancelled=true}},[requested]);useEffect(()=>{const listener=(event:Event)=>setState((event as CustomEvent<GraphSourceMode>).detail);window.addEventListener(EVENT,listener);return()=>window.removeEventListener(EVENT,listener)},[]);return{source,setSource}}
