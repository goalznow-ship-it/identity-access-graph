import { useState } from 'react'; import { motion } from 'framer-motion'; import { useGraphData } from '../hooks/useGraphData'; import { DashboardEmpty,DashboardGrid,DashboardHeader,DashboardSkeleton } from '../components/dashboard'
function DashboardContent({onRefresh,refreshing,source,onSourceChange,importId}:{onRefresh:()=>void;refreshing:boolean;source:'mock'|'imported';onSourceChange:(source:'mock'|'imported')=>void;importId:string|null}){const{data,loading,error}=useGraphData(source==='imported'?importId:null);if(loading)return <DashboardSkeleton/>;if(error||!data)return <DashboardEmpty onRetry={onRefresh}/>;return <><DashboardHeader nodes={data.nodes.length} onRefresh={onRefresh} refreshing={refreshing} source={source} onSourceChange={onSourceChange} importedAvailable={Boolean(importId)}/><div className="mt-5"><DashboardGrid data={data}/></div></>}
export function Home(){const[revision,setRevision]=useState(0);const[refreshing,setRefreshing]=useState(false);const[source,setSource]=useState<'mock'|'imported'>('mock');const importId=typeof localStorage==='undefined'?null:localStorage.getItem('lastImportId');const refresh=()=>{setRefreshing(true);setRevision((value)=>value+1);window.setTimeout(()=>setRefreshing(false),700)};return <motion.div initial={{opacity:0}} animate={{opacity:1}} transition={{duration:.25}} className="mx-auto max-w-[1800px] pb-8"><DashboardContent key={`${revision}-${source}`} onRefresh={refresh} refreshing={refreshing} source={source} onSourceChange={setSource} importId={importId}/></motion.div>}
