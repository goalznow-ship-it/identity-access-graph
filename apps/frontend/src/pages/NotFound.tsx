import { useNavigate } from 'react-router-dom'; import { HelpCircle } from 'lucide-react'; import { Card } from '../components/ui/Card'
export function NotFoundPage() { const navigate = useNavigate(); return <div className="flex min-h-[65vh] items-center justify-center"><Card className="max-w-md p-8 text-center"><HelpCircle className="mx-auto h-9 w-9 text-primary"/><h1 className="mt-4 text-xl font-semibold text-gray-100">Page not found</h1><p className="mt-2 text-sm text-gray-500">This route is not part of the Identity Access Graph workspace.</p><button onClick={() => navigate('/')} className="mt-5 rounded-lg bg-primary px-4 py-2 text-sm text-white">Return to dashboard</button></Card></div> }
