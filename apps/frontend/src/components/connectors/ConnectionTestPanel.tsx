export function ConnectionTestPanel({result,error}:{result?:{connected:boolean;namingContexts:string[]};error?:string}){if(error)return <div role="alert"className="rounded bg-danger/10 p-3 text-xs text-danger">{error}</div>;if(!result)return null;return <div className="rounded bg-success/10 p-3 text-xs text-success">Connection successful · {result.namingContexts.join(', ')||'Root DSE available'}</div>}
