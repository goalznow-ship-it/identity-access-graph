import type{SyncRun}from'../../types/connector';export function SyncRunDetails({run,onClose}:{run?:SyncRun;onClose:()=>void}){if(!run)return null;return <aside className="fixed inset-y-0 right-0 z-50 w-[min(26rem,100vw)]overflow-y-auto border-l border-border bg-surface p-5 shadow-2xl"><button onClick={onClose}aria-label="Close sync details"className="float-right">×</button><h2 className="text-lg font-semibold">Sync run</h2><p className="mt-2 text-sm">{run.status} · {run.durationMs??0} ms</p><pre className="mt-4 overflow-auto rounded bg-card p-3 text-xs">{JSON.stringify(run.objectCounts,null,2)}</pre>{run.errors.map(error=><p key={error}className="mt-2 text-xs text-danger">{error}</p>)}</aside>}
