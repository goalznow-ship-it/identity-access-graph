import type{SyncRun}from'../../types/connector';export function SyncRunList({runs,onSelect}:{runs:SyncRun[];onSelect:(run:SyncRun)=>void}){if(!runs.length)return <div className="rounded border border-dashed border-border p-6 text-center text-xs text-gray-500">No synchronization runs yet.</div>;return <div className="space-y-2">{runs.map(run=><button key={run.syncRunId}onClick={()=>onSelect(run)}className="flex w-full justify-between rounded bg-surface p-3 text-left text-xs"><span>{run.mode} · {new Date(run.startedAt).toLocaleString()}</span><b>{run.status}</b></button>)}</div>}
