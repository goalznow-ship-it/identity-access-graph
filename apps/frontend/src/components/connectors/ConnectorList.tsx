import type{Connector}from'../../types/connector';import{ConnectorCard}from'./ConnectorCard';export function ConnectorList({connectors,actions}:{connectors:Connector[];actions:(connector:Connector)=>any}){if(!connectors.length)return <div className="rounded-xl border border-dashed border-border p-10 text-center text-sm text-gray-500">No connectors configured. Create an AD or generic LDAP connector to begin.</div>;return <div className="grid gap-3 lg:grid-cols-2">{connectors.map(connector=><ConnectorCard key={connector.id}connector={connector}{...actions(connector)}/>)}</div>}
