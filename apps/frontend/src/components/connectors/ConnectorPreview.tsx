import type{SyncRun}from'../../types/connector';export function ConnectorPreview({run}:{run?:SyncRun}){if(!run)return null;return <section className="rounded-xl border border-border bg-card p-4"><h2 className="text-sm font-semibold">Preview</h2><div className="mt-3 flex flex-wrap gap-2">{Object.entries(run.objectCounts).map(([type,count])=><span key={type}className="rounded bg-surface px-2 py-1 text-xs">{type}: <b>{count}</b></span>)}</div>{run.warnings.map(warning=><p key={warning}className="mt-2 text-xs text-warning">{warning}</p>)}</section>}
