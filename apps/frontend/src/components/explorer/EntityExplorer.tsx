import{useEffect,useMemo,useState}from'react';import{useNavigate}from'react-router-dom';import{Search}from'../icons';import{useGraphSource}from'../../hooks/useGraphSource';import{useGraphData}from'../../hooks/useGraphData';import{exploreNodes,exportNodes}from'../../services/explorerApi';import type{GraphNode}from'../../types/graph';import{PageTitle}from'../../pages/Identities'
export function EntityExplorer({title,subtitle,nodeTypes}:{title:string;subtitle:string;nodeTypes:string[]}){const{source}=useGraphSource(),local=useGraphData(undefined,source==='neo4j'?'mock':source),navigate=useNavigate(),[items,setItems]=useState<GraphNode[]>([]),[total,setTotal]=useState(0),[search,setSearch]=useState(''),[risk,setRisk]=useState(''),[system,setSystem]=useState(''),[sortBy,setSortBy]=useState('displayName'),[direction,setDirection]=useState<'ASC'|'DESC'>('ASC'),[offset,setOffset]=useState(0),[loading,setLoading]=useState(true),[error,setError]=useState('');const limit=25;useEffect(()=>{setOffset(0)},[search,risk,system,sortBy,direction,nodeTypes.join(',')]);useEffect(()=>{let cancelled=false;setLoading(true);setError('');const run=async()=>{try{if(source==='neo4j'){const page=await exploreNodes({q:search,nodeTypes,riskLevels:risk?[risk]:[],sourceSystems:system?[system]:[],limit,offset,sortBy,sortDirection:direction});if(!cancelled){setItems(page.items);setTotal(page.total)}}else{const all=(local.data?.nodes??[]).filter(node=>nodeTypes.includes(node.nodeType)).filter(node=>!search||`${node.displayName} ${node.id} ${node.properties?.username??''} ${node.properties?.email??''}`.toLowerCase().includes(search.toLowerCase())).filter(node=>!risk||node.riskLevel===risk).filter(node=>!system||node.sourceSystem===system).sort((a,b)=>String((a as any)[sortBy]??a.properties?.[sortBy]??'').localeCompare(String((b as any)[sortBy]??b.properties?.[sortBy]??''))* (direction==='ASC'?1:-1));if(!cancelled){setTotal(all.length);setItems(all.slice(offset,offset+limit))}}}catch(e){if(!cancelled)setError((e as Error).message)}finally{if(!cancelled)setLoading(false)}};if(source!=='neo4j'&&local.loading)return;void run();return()=>{cancelled=true}},[source,local.data,local.loading,search,risk,system,sortBy,direction,offset,nodeTypes.join(',')]);const systems=useMemo(()=>[...new Set((local.data?.nodes??[]).map(node=>node.sourceSystem))].sort(),[local.data]);return <div className="space-y-5"><PageTitle title={title}subtitle={`${total} ${subtitle}`}/><div className="flex flex-wrap gap-2 rounded-xl border border-border bg-card p-3"><label className="flex min-w-64 flex-1 items-center gap-2"><Search className="h-4 w-4 text-gray-500"/><input aria-label={`Search ${title}`}value={search}onChange={e=>setSearch(e.target.value)}placeholder="Search name, identifier, username, or email"className="w-full bg-transparent text-sm outline-none"/></label><select aria-label="Risk filter"value={risk}onChange={e=>setRisk(e.target.value)}className="rounded border border-border bg-surface px-2 text-xs"><option value="">All risks</option>{['NONE','LOW','MEDIUM','HIGH','CRITICAL'].map(x=><option key={x}>{x}</option>)}</select><select aria-label="Source filter"value={system}onChange={e=>setSystem(e.target.value)}className="rounded border border-border bg-surface px-2 text-xs"><option value="">All sources</option>{systems.map(x=><option key={x}>{x}</option>)}</select><select aria-label="Sort field"value={sortBy}onChange={e=>setSortBy(e.target.value)}className="rounded border border-border bg-surface px-2 text-xs"><option value="displayName">Name</option><option value="nodeType">Type</option><option value="sourceSystem">Source</option><option value="riskLevel">Risk</option><option value="status">Status</option></select><button onClick={()=>setDirection(value=>value==='ASC'?'DESC':'ASC')}className="rounded border border-border px-3 text-xs">{direction==='ASC'?'Ascending':'Descending'}</button><button onClick={()=>exportNodes(items,'json')}className="text-xs text-primary">JSON</button><button onClick={()=>exportNodes(items,'csv')}className="text-xs text-primary">CSV</button></div>{error&&<div role="alert"className="rounded border border-danger/40 p-4 text-sm text-danger">{error}<button onClick={()=>setOffset(value=>value)}className="ml-3 underline">Retry</button></div>}{loading?<div className="h-48 animate-pulse rounded-xl bg-white/5"/>:items.length===0?<div className="rounded-xl border border-dashed border-border p-10 text-center text-sm text-gray-500">No records match the selected filters.</div>:<div className="overflow-x-auto rounded-xl border border-border"><table className="w-full text-left text-xs"><thead className="bg-surface text-gray-500"><tr>{['Name','Type','Username / ID','Status','Risk','Source'].map(x=><th key={x}className="px-4 py-3">{x}</th>)}</tr></thead><tbody className="divide-y divide-border">{items.map(node=><tr key={node.id}tabIndex={0}onClick={()=>navigate(['USER','LINUX_USER'].includes(node.nodeType)?`/identities/${encodeURIComponent(node.id)}`:`/graph?nodeId=${encodeURIComponent(node.id)}`)}className="cursor-pointer hover:bg-white/[.03]"><td className="px-4 py-3 font-medium text-gray-200">{node.displayName}</td><td className="px-4">{node.nodeType}</td><td className="px-4 text-gray-400">{String(node.properties?.username??node.sourceId??node.id)}</td><td className="px-4">{String(node.properties?.status??'—')}</td><td className="px-4">{node.riskLevel}</td><td className="px-4">{node.sourceSystem}</td></tr>)}</tbody></table></div>}<div className="flex items-center justify-between text-xs text-gray-500"><span>{total?`${offset+1}–${Math.min(offset+items.length,total)} of ${total}`:'0 records'}</span><div className="flex gap-2"><button disabled={offset===0||loading}onClick={()=>setOffset(Math.max(0,offset-limit))}className="rounded border border-border px-3 py-2 disabled:opacity-40">Previous</button><button disabled={offset+items.length>=total||loading}onClick={()=>setOffset(offset+limit)}className="rounded border border-border px-3 py-2 disabled:opacity-40">Next</button></div></div></div>}
