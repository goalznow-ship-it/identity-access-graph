import type{RiskSummary}from'../../types/risk';export function RiskSummaryCards({summary}:{summary:RiskSummary}){return <div className="grid grid-cols-2 gap-3 lg:grid-cols-5">{[['Open',summary.openFindings],['Critical',summary.countsBySeverity.CRITICAL??0],['High',summary.countsBySeverity.HIGH??0],['Total',summary.totalFindings],['Scan time',`${summary.scanDurationMs} ms`]].map(([label,value])=><div key={label}className="rounded-xl border border-border bg-card p-4"><div className="text-xs uppercase tracking-wider text-gray-500">{label}</div><div className="mt-2 text-2xl font-semibold text-gray-100">{value}</div></div>)}</div>}
