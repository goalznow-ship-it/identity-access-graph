import type{RiskSummary}from'../../types/risk';const colors:Record<string,string>={CRITICAL:'#ef4444',HIGH:'#f97316',MEDIUM:'#eab308',LOW:'#22c55e',INFO:'#38bdf8'};export function RiskSeverityChart({summary}:{summary:RiskSummary}){const max=Math.max(1,...Object.values(summary.countsBySeverity));return <section className="rounded-xl border border-border bg-card p-4"><h2 className="text-sm font-semibold">Severity</h2><div className="mt-4 space-y-2">{Object.entries(colors).map(([key,color])=><div key={key}className="grid grid-cols-[5rem_1fr_2rem] items-center gap-2 text-xs"><span>{key}</span><div className="h-2 rounded bg-white/5"><div className="h-full rounded"style={{width:`${(summary.countsBySeverity[key]??0)/max*100}%`,background:color}}/></div><b>{summary.countsBySeverity[key]??0}</b></div>)}</div></section>}
