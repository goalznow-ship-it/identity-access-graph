import type{RiskFinding}from'../../types/risk';export function RiskFindingDetails({finding,onClose}:{finding:RiskFinding|null;onClose:()=>void}){if(!finding)return null;return <aside className="fixed inset-y-0 right-0 z-50 w-[min(26rem,100vw)] overflow-y-auto border-l border-border bg-surface p-5 shadow-2xl"><button onClick={onClose}aria-label="Close finding details"className="float-right text-gray-500">×</button><div className="text-xs font-semibold text-warning">{finding.severity} · {finding.score}/100</div><h2 className="mt-2 text-xl font-semibold">{finding.title}</h2><p className="mt-3 text-sm leading-6 text-gray-400">{finding.description}</p><h3 className="mt-5 text-xs font-semibold uppercase text-gray-500">Score factors</h3><div className="mt-2 space-y-2">{finding.scoreFactors.map(f=><div key={f.factor}className="flex justify-between rounded bg-card p-2 text-xs"><span>{f.reason}</span><b>+{f.score}</b></div>)}</div><h3 className="mt-5 text-xs font-semibold uppercase text-gray-500">Remediation</h3><p className="mt-2 text-sm text-gray-300">{finding.remediationGuidance}</p></aside>}
