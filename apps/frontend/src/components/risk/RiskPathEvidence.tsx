import{Link}from'react-router-dom';export function RiskPathEvidence({paths}:{paths:string[][]}){if(!paths.length)return null;return <div className="mt-3"><h3 className="text-[10px] font-semibold uppercase text-gray-500">Evidence path</h3>{paths.map((path,index)=><div key={index}className="mt-1 flex flex-wrap items-center gap-1 text-xs">{path.map((id,i)=><span key={id+i}className="flex items-center gap-1"><Link className="text-primary hover:underline"to={`/graph?nodeId=${encodeURIComponent(id)}`}>{id}</Link>{i<path.length-1&&<span>→</span>}</span>)}</div>)}</div>}
