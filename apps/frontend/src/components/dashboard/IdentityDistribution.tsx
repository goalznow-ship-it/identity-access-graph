import { memo } from 'react'
const segments = [{ name: 'AD', value: 58, color: '#3b82f6' }, { name: 'FreeIPA', value: 18, color: '#8b5cf6' }, { name: 'Linux', value: 15, color: '#14b8a6' }, { name: 'Imported', value: 9, color: '#f59e0b' }]
export const IdentityDistribution = memo(function IdentityDistribution() { let offset = 0; return <Panel title="Identity Distribution"><div className="flex items-center justify-center gap-7"><svg viewBox="0 0 42 42" className="h-32 w-32 -rotate-90">{segments.map((segment) => { const dash = `${segment.value} ${100-segment.value}`; const current = offset; offset += segment.value; return <circle key={segment.name} cx="21" cy="21" r="15.9" fill="none" stroke={segment.color} strokeWidth="5" strokeDasharray={dash} strokeDashoffset={-current} /> })}<circle cx="21" cy="21" r="12" fill="#111827" /></svg><div className="space-y-2">{segments.map((segment) => <div key={segment.name} className="flex w-28 items-center justify-between text-xs"><span className="flex items-center gap-2 text-gray-400"><i className="h-2 w-2 rounded-full" style={{ background: segment.color }} />{segment.name}</span><b className="text-gray-200">{segment.value}%</b></div>)}</div></div></Panel> })
export function Panel({ title, children, className = '' }: { title: string; children: React.ReactNode; className?: string }) { return <section className={`rounded-xl border border-border bg-card/75 p-5 shadow-glass ${className}`}><h2 className="mb-4 text-sm font-semibold text-gray-200">{title}</h2>{children}</section> }
