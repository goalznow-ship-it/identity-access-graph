import { memo } from 'react'; import { Panel } from './IdentityDistribution'
const risks = [{ label: 'Critical', value: 12, color: '#ef4444' }, { label: 'High', value: 38, color: '#f97316' }, { label: 'Medium', value: 96, color: '#eab308' }, { label: 'Low', value: 184, color: '#22c55e' }]
export const RiskOverview = memo(function RiskOverview() { return <Panel title="Risk Overview"><div className="space-y-4">{risks.map((risk) => <div key={risk.label}><div className="mb-1.5 flex justify-between text-xs"><span className="text-gray-400">{risk.label}</span><b style={{ color: risk.color }}>{risk.value}</b></div><div className="h-2 overflow-hidden rounded-full bg-white/5"><div className="h-full rounded-full" style={{ width: `${Math.max(8, risk.value / 2)}%`, background: risk.color }} /></div></div>)}</div></Panel> })
