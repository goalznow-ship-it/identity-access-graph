import { memo } from 'react'; import { Panel } from './IdentityDistribution'
const metrics = [{ label: 'MFA coverage', value: 92, tone: '#22c55e' }, { label: 'Disabled accounts reviewed', value: 78, tone: '#38bdf8' }, { label: 'Orphan identities resolved', value: 64, tone: '#f59e0b' }, { label: 'Privileged accounts certified', value: 86, tone: '#8b5cf6' }]
export const ComplianceOverview = memo(function ComplianceOverview() { return <Panel title="Compliance"><div className="grid grid-cols-2 gap-3">{metrics.map((metric) => <div key={metric.label} className="rounded-lg border border-border bg-surface/60 p-3"><div className="text-xl font-semibold" style={{ color: metric.tone }}>{metric.value}%</div><div className="mt-1 text-[10px] leading-4 text-gray-500">{metric.label}</div><div className="mt-2 h-1 rounded bg-white/5"><div className="h-full rounded" style={{ width: `${metric.value}%`, background: metric.tone }} /></div></div>)}</div></Panel> })
