import { Server as DatabaseZap } from 'lucide-react'
export function DashboardEmpty({ onRetry, empty = false }: { onRetry: () => void; empty?: boolean }) { return <div className="flex min-h-[60vh] items-center justify-center"><div className="rounded-xl border border-border bg-card p-8 text-center"><DatabaseZap className="mx-auto h-8 w-8 text-primary"/><h2 className="mt-3 text-lg font-semibold text-gray-200">{empty?'No dataset has been imported yet.':'Dashboard data unavailable'}</h2><p className="mt-1 text-sm text-gray-500">{empty?'Import a dataset to populate the identity graph and security dashboard.':'The identity dataset could not be loaded.'}</p><button onClick={onRetry} className="mt-4 rounded-lg bg-primary px-4 py-2 text-xs text-white">{empty?'Check again':'Retry synchronization'}</button></div></div> }
