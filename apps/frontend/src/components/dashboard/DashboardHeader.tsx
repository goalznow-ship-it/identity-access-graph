import { RefreshCw,Shield } from 'lucide-react';import { Link } from 'react-router-dom';import { Badge } from '../ui/Badge';import type { GraphSourceMode } from '../../types/neo4j';import { Neo4jStatusBadge } from '../Neo4jStatusBadge'
export function DashboardHeader({nodes,onRefresh,refreshing,source,onSourceChange,importedAvailable}:{nodes:number;onRefresh:()=>void;refreshing:boolean;source:GraphSourceMode;onSourceChange:(source:GraphSourceMode)=>void;importedAvailable:boolean}){return <header className="flex flex-col gap-4 border-b border-border pb-5 lg:flex-row lg:items-end lg:justify-between"><div><div className="flex items-center gap-2 text-xs font-medium uppercase tracking-[.18em] text-primary"><Shield className="h-4 w-4"/> Identity security operations</div><h1 className="mt-2 text-3xl font-semibold tracking-tight text-gray-100">Enterprise Control Center</h1><p className="mt-1 text-sm text-gray-500">Unified identity posture, access intelligence and operational health</p></div><div className="flex flex-wrap items-center gap-2 text-xs"><Badge variant="success">Production</Badge><select aria-label="Dashboard graph source" value={source} onChange={(event)=>onSourceChange(event.target.value as GraphSourceMode)} className="rounded-full border border-border bg-surface px-3 py-1 text-gray-300"><option value="mock">Mock Enterprise</option><option value="imported" disabled={!importedAvailable}>Imported Session</option><option value="neo4j">Neo4j Live</option></select>{source==='neo4j'&&<Neo4jStatusBadge details/>}<span className="rounded-full border border-border px-3 py-1 text-gray-400">Current dataset · {nodes.toLocaleString()} objects</span><Link to={`/graph${source==='neo4j'?'?source=neo4j':''}`} className="text-primary hover:underline">Open graph</Link><Link to="/imports" className="text-primary hover:underline">Imports</Link><Link to="/pipeline" className="text-primary hover:underline">Pipeline</Link><button onClick={onRefresh} disabled={refreshing} className="rounded-lg border border-border bg-card p-2 text-gray-400 hover:text-white disabled:cursor-wait" title={refreshing?'Refreshing dashboard':'Refresh dashboard'}><RefreshCw className={`h-4 w-4 ${refreshing?'animate-spin':''}`}/></button></div></header>}
