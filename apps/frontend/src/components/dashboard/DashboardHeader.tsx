import { RefreshCw,Shield } from 'lucide-react'; import { Link } from 'react-router-dom'; import { Badge } from '../ui/Badge'
export function DashboardHeader({nodes,onRefresh,refreshing,source,onSourceChange,importedAvailable}:{nodes:number;onRefresh:()=>void;refreshing:boolean;source:'mock'|'imported';onSourceChange:(source:'mock'|'imported')=>void;importedAvailable:boolean}){return <header className="flex flex-col gap-4 border-b border-border pb-5 lg:flex-row lg:items-end lg:justify-between"><div><div className="flex items-center gap-2 text-xs font-medium uppercase tracking-[.18em] text-primary"><Shield className="h-4 w-4"/> Identity security operations</div><h1 className="mt-2 text-3xl font-semibold tracking-tight text-gray-100">Enterprise Control Center</h1><p className="mt-1 text-sm text-gray-500">Unified identity posture, access intelligence and operational health</p></div><div className="flex flex-wrap items-center gap-2 text-xs"><Badge variant="success">Production</Badge><label className="sr-only" htmlFor="dashboard-source">Graph source</label><select id="dashboard-source" value={source} onChange={(event)=>onSourceChange(event.target.value as 'mock'|'imported')} className="rounded-full border border-border bg-surface px-3 py-1 text-gray-300"><option value="mock">Mock graph</option><option value="imported" disabled={!importedAvailable}>Imported graph{!importedAvailable?' — no active import':''}</option></select><span className="rounded-full border border-border px-3 py-1 text-gray-400">Enterprise dataset · {nodes.toLocaleString()} objects</span><span className="text-gray-500">Synchronized 2 minutes ago</span><Link to="/graph" className="text-primary hover:underline">Open graph</Link><Link to="/imports" className="text-primary hover:underline">Imports</Link><Link to="/pipeline" className="text-primary hover:underline">Pipeline</Link><button onClick={onRefresh} disabled={refreshing} className="rounded-lg border border-border bg-card p-2 text-gray-400 hover:text-white disabled:cursor-wait" title={refreshing?'Refreshing dashboard':'Refresh dashboard'} aria-label="Refresh dashboard"><RefreshCw className={`h-4 w-4 ${refreshing?'animate-spin':''}`}/></button></div></header>}
