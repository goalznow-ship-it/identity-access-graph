import { Search, Upload, Share2, HelpCircle, GitMerge, Activity, GitBranch } from 'lucide-react'; import { Link } from 'react-router-dom'; import { Panel } from './IdentityDistribution'
export const QUICK_ACTIONS = [{ label:'Import Data',path:'/imports',icon:Upload },{ label:'Open Graph',path:'/graph',icon:Share2 },{ label:'Business Questions',path:'/business-questions',icon:HelpCircle },{ label:'Identity Search',path:'/identities',icon:Search },{ label:'Run Correlation',path:'/imports?step=correlation',icon:GitMerge },{ label:'Investigate',path:'/graph?mode=investigation',icon:Activity },{ label:'Open Pipeline',path:'/pipeline',icon:GitBranch }]
export function QuickActions() { return <Panel title="Quick Actions"><div className="grid grid-cols-2 gap-2 sm:grid-cols-4 xl:grid-cols-7">{QUICK_ACTIONS.map(({label,path,icon:Icon}) => <Link key={label} to={path} className="flex min-h-20 flex-col items-center justify-center gap-2 rounded-lg border border-border bg-surface/50 px-2 text-[11px] text-gray-400 hover:border-primary/40 hover:bg-primary/5 hover:text-white focus:outline-none focus:ring-2 focus:ring-primary"><Icon className="h-5 w-5 text-primary"/>{label}</Link>)}</div></Panel> }
