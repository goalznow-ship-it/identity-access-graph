import { Search, Upload, Share2 as Network, HelpCircle as MessageSquareText, GitMerge, Activity as Radar, GitBranch as Workflow } from 'lucide-react'; import { useNavigate } from 'react-router-dom'; import { Panel } from './IdentityDistribution'
const actions = [{ label: 'Import', path: '/imports', icon: Upload }, { label: 'Open Graph', path: '/graph', icon: Network }, { label: 'Business Questions', path: '/business-questions', icon: MessageSquareText }, { label: 'Identity Search', path: '/graph', icon: Search }, { label: 'Run Correlation', path: '/imports', icon: GitMerge }, { label: 'Investigate', path: '/graph', icon: Radar }, { label: 'Open Pipeline', path: '/pipeline', icon: Workflow }]
export function QuickActions() { const navigate = useNavigate(); return <Panel title="Quick Actions"><div className="grid grid-cols-2 gap-2 sm:grid-cols-4 xl:grid-cols-7">{actions.map(({ label, path, icon: Icon }) => <button key={label} onClick={() => navigate(path)} className="flex min-h-20 flex-col items-center justify-center gap-2 rounded-lg border border-border bg-surface/50 px-2 text-[11px] text-gray-400 transition hover:border-primary/40 hover:bg-primary/5 hover:text-white"><Icon className="h-5 w-5 text-primary" />{label}</button>)}</div></Panel> }
