import { FileText as Bookmark, Share2 as Eye, Search, Shield as Swords } from 'lucide-react'; import { Panel } from './IdentityDistribution'
const items = [{ icon: Bookmark, type: 'Bookmark', title: 'Domain Admin privilege chain', meta: '12 objects · updated 20m ago' }, { icon: Eye, type: 'Saved view', title: 'Finance application access', meta: '38 identities · updated 1h ago' }, { icon: Search, type: 'Recent search', title: 'Dormant privileged identities', meta: '9 results · searched 2h ago' }, { icon: Swords, type: 'Attack simulation', title: 'Linux sudo escalation path', meta: 'Risk score 86 · simulated yesterday' }]
export function RecentInvestigations() { return <Panel title="Recent Investigations"><div className="grid gap-2 md:grid-cols-2 xl:grid-cols-4">{items.map(({ icon: Icon, type, title, meta }) => <article key={title} className="rounded-lg border border-border bg-surface/50 p-3"><div className="flex items-center gap-2 text-[10px] uppercase tracking-wide text-primary"><Icon className="h-3.5 w-3.5" />{type}</div><p className="mt-2 text-xs font-medium text-gray-300">{title}</p><p className="mt-1 text-[10px] text-gray-600">{meta}</p></article>)}</div></Panel> }
