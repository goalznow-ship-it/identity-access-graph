import { useNeo4jHealth } from '../hooks/useNeo4jHealth'
const tones={connected:'text-success bg-success/10',disabled:'text-gray-400 bg-white/5',unreachable:'text-danger bg-danger/10',degraded:'text-warning bg-warning/10',checking:'text-primary bg-primary/10'}
export function Neo4jStatusBadge({details=false}:{details?:boolean}){const{health,state,retry}=useNeo4jHealth();return <button onClick={()=>void retry()} title={health?`${health.database} · ${health.serverAgent??'server unknown'} · ${health.latencyMs}ms · checked ${new Date(health.timestamp).toLocaleTimeString()}`:'Check Neo4j connectivity'} className={`rounded-full px-2.5 py-1 text-[10px] font-medium ${tones[state]}`}><span className="mr-1">●</span>Neo4j {state[0].toUpperCase()+state.slice(1)}{details&&health?` · ${health.database} · ${health.latencyMs}ms`:''}</button>}
