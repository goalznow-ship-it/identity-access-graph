import type{AttackPath}from'../../types/attackPath';import{AttackPathCard}from'./AttackPathCard';export function AttackPathList({paths,onSelect}:{paths:AttackPath[];onSelect:(path:AttackPath)=>void}){if(!paths.length)return <div className="rounded-xl border border-dashed border-border p-10 text-center text-sm text-gray-500">No privilege-reachability paths match this analysis.</div>;return <div className="space-y-3">{paths.map(path=><AttackPathCard key={path.id}path={path}onSelect={()=>onSelect(path)}/>)}</div>}
