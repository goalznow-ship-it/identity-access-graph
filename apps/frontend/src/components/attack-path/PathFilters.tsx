export function PathFilters({type,minScore,onType,onScore}:{type:string;minScore:number;onType:(value:string)=>void;onScore:(value:number)=>void}){return <div className="flex gap-2"><select aria-label="Filter path type"value={type}onChange={e=>onType(e.target.value)}className="rounded border border-border bg-card px-3 py-2 text-xs"><option value="">All path types</option>{['DOMAIN_ADMIN','TIER_ZERO','SUDO_ROOT','PRODUCTION_HOST','FINANCE_APPLICATION','CRITICAL_DATABASE','BUSINESS_SERVICE','CROSS_DOMAIN'].map(x=><option key={x}>{x}</option>)}</select><label className="flex items-center gap-2 rounded border border-border bg-card px-3 text-xs">Minimum risk<input aria-label="Minimum risk"type="number"min="0"max="100"value={minScore}onChange={e=>onScore(Number(e.target.value))}className="w-14 bg-transparent"/></label></div>}
