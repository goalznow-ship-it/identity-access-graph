import type{AttackPath}from'../../types/attackPath';export function AttackPathGraph({path}:{path:AttackPath}){return <div className="flex flex-wrap items-center gap-2 rounded-lg bg-surface p-3">{path.nodes.map((node,index)=><div key={node.id}className="flex items-center gap-2"><span className="rounded-lg border border-warning/40 bg-warning/10 px-3 py-2 text-xs">{node.displayName}</span>{index<path.relationships.length&&<span className="text-[10px] text-warning">→ {path.relationships[index].relationshipType} →</span>}</div>)}</div>}
