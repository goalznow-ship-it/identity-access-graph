import { Controller, Get } from '@nestjs/common'; import { Neo4jService } from './neo4j.service'; import type { Neo4jHealthResult } from './neo4j.types'
@Controller('health/neo4j') export class Neo4jHealthController{constructor(private readonly neo4j:Neo4jService){}@Get()async check():Promise<Neo4jHealthResult>{const started=Date.now();if(!this.neo4j.isEnabled())return{status:'disabled',connectivity:false,database:this.neo4j.config.database,latencyMs:Date.now()-started,timestamp:new Date().toISOString()};try{const driver=this.neo4j.connect();const info=await driver.getServerInfo({database:this.neo4j.config.database});return{status:'ok',connectivity:true,database:this.neo4j.config.database,serverAgent:info.agent,latencyMs:Date.now()-started,timestamp:new Date().toISOString()}}catch{return{status:'error',connectivity:false,database:this.neo4j.config.database,latencyMs:Date.now()-started,timestamp:new Date().toISOString()}}}}
