import{Injectable}from'@nestjs/common';import{Neo4jService}from'../neo4j';import{RiskGraphSourceService,type GraphSnapshot}from'../risk';import type{AttackPath,AttackPathSearch}from'./attack-path.types'
export const ALLOWED_RELATIONSHIPS=['MEMBER_OF','HAS_ROLE','GRANTS','HAS_ACCESS_TO','AUTHENTICATES_TO','HAS_SUDO_POLICY','HAS_SSH_KEY','TRUSTS','BELONGS_TO','REPORTS_TO','MANAGED_BY','RUNS_ON','SUPPORTS','USES','EXISTS_IN']
@Injectable()export class AttackPathRepository{private paths=new Map<string,AttackPath>();private analysis?:{at:string;duration:number};constructor(private neo4j:Neo4jService,private source:RiskGraphSourceService){}save(paths:AttackPath[],duration:number){paths.forEach(path=>this.paths.set(path.id,path));this.analysis={at:new Date().toISOString(),duration}}get(id:string){return this.paths.get(id)}list(){return[...this.paths.values()].sort((a,b)=>b.totalRiskScore-a.totalRiskScore||a.id.localeCompare(b.id))}latest(){return this.analysis}async graph(request:AttackPathSearch):Promise<GraphSnapshot>{if(request.graphSource==='memory'||(request.graphSource!=='neo4j'&&!this.neo4j.isEnabled()))return this.source.load('memory');const depth=Math.min(12,Math.max(1,Math.trunc(request.maxDepth??6))),limit=Math.min(500,Math.max(1,Math.trunc(request.maxPaths??50)*20));const directed=request.directed!==false;const pattern=directed?`-[rels*1..${depth}]->`:`-[rels*1..${depth}]-`;const query=`MATCH path=(source:GraphNode)${pattern}(target:GraphNode) WHERE ($sourceId IS NULL OR source.id=$sourceId) AND ($targetId IS NULL OR target.id=$targetId) AND ALL(rel IN rels WHERE type(rel) IN $types) RETURN [n IN nodes(path) | n{.*, properties: properties(n)}] AS nodes, [r IN relationships(path) | r{.*, source: startNode(r).id, target: endNode(r).id, relationshipType: type(r), properties: properties(r)}] AS relationships LIMIT $limit`;const result=await this.neo4j.read(query,{sourceId:request.sourceNodeId??null,targetId:request.targetNodeId??null,types:this.allowed(request.allowedRelationshipTypes),limit});const nodes=new Map<string,any>(),relationships=new Map<string,any>();result.records.forEach((record:any)=>{(record.get('nodes')??[]).forEach((node:any)=>nodes.set(node.id,node));(record.get('relationships')??[]).forEach((rel:any)=>relationships.set(rel.id,rel))});return{nodes:[...nodes.values()],relationships:[...relationships.values()]}}allowed(requested?:string[]){return(requested?.length?requested:ALLOWED_RELATIONSHIPS).filter(type=>ALLOWED_RELATIONSHIPS.includes(type))}}
