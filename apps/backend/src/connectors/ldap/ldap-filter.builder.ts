export function escapeLdapFilter(value:string){return value.replace(/\\/g,'\\5c').replace(/\*/g,'\\2a').replace(/\(/g,'\\28').replace(/\)/g,'\\29').replace(/\0/g,'\\00')}export const andFilter=(...filters:string[])=>`(&${filters.filter(Boolean).join('')})`;export const changedSinceFilter=(iso:string)=>`(whenChanged>=${escapeLdapFilter(iso.replace(/[-:]/g,'').replace(/\.\d{3}Z$/,'Z'))})`
