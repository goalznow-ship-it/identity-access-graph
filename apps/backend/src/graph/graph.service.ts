import { Inject,Injectable,ServiceUnavailableException } from '@nestjs/common'; import { Neo4jService } from '../neo4j'; import { GRAPH_REPOSITORY,GraphRepository } from './repositories'; import type { NeighborOptions,PersistedGraphNode,PersistedGraphRelationship,SearchOptions,SubgraphOptions } from './repositories'
@Injectable() export class GraphService{constructor(@Inject(GRAPH_REPOSITORY)private readonly repository:GraphRepository,private readonly neo4j:Neo4jService){}private available(){if(!this.neo4j.isEnabled())throw new ServiceUnavailableException('Graph persistence is disabled')}getNodeById(id:string){this.available();return this.repository.getNodeById(id)}getNeighbors(id:string,options?:NeighborOptions){this.available();return this.repository.getNeighbors(id,options)}searchNodes(query:string,options?:SearchOptions){this.available();return this.repository.searchNodes(query,options)}getSubgraph(ids:string[],options?:SubgraphOptions){this.available();return this.repository.getSubgraph(ids,options)}upsertNodes(nodes:PersistedGraphNode[]){this.available();return this.repository.upsertNodes(nodes)}upsertRelationships(relationships:PersistedGraphRelationship[]){this.available();return this.repository.upsertRelationships(relationships)}}
