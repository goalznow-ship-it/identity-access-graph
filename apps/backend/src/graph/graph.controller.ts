import { Body,Controller,Get,HttpCode,NotFoundException,Param,Post,Query } from '@nestjs/common'; import { GraphService } from './graph.service'; import type { PersistedGraphNode,PersistedGraphRelationship } from './repositories'
@Controller('graph') export class GraphController{constructor(private readonly graph:GraphService){}@Get('nodes/:id')async node(@Param('id')id:string){const node=await this.graph.getNodeById(id);if(!node)throw new NotFoundException('Graph node not found');return node}@Get('nodes/:id/neighbors')neighbors(@Param('id')id:string,@Query('direction')direction?:'incoming'|'outgoing'|'both',@Query('limit')limit?:string){return this.graph.getNeighbors(id,{direction,limit:limit?Number(limit):undefined})}@Get('search')search(@Query('q')query='',@Query('nodeType')nodeType?:string,@Query('sourceSystem')sourceSystem?:string,@Query('risk')risk?:string,@Query('limit')limit?:string,@Query('offset')offset?:string){return this.graph.searchNodes(query,{nodeTypes:nodeType?[nodeType]:[],sourceSystems:sourceSystem?[sourceSystem]:[],riskLevels:risk?[risk]:[],limit:limit?Number(limit):undefined,offset:offset?Number(offset):undefined})}@Get('subgraph')subgraph(@Query('ids')ids='',@Query('depth')depth?:string,@Query('limit')limit?:string){return this.graph.getSubgraph(ids.split(',').filter(Boolean),{depth:depth?Number(depth):undefined,limit:limit?Number(limit):undefined})}@Post('nodes/batch')@HttpCode(200)nodes(@Body()body:{nodes:PersistedGraphNode[]}){return this.graph.upsertNodes(body.nodes??[])}@Post('relationships/batch')@HttpCode(200)relationships(@Body()body:{relationships:PersistedGraphRelationship[]}){return this.graph.upsertRelationships(body.relationships??[])}}
